--- a/drivers/net/ethernet/airoha/airoha_npu.c
+++ b/drivers/net/ethernet/airoha/airoha_npu.c
@@ -796,10 +796,6 @@ static int airoha_npu_probe(struct platf
 	struct device_node *np;
 	void __iomem *base;
 	int i, irq, err;
-#ifdef CONFIG_NET_AIROHA_SOC_NPU	
-	//unsigned int isMailBoxSuccess = 0;
-	int irq_idx;
-#endif
 	int index;
 
 	base = devm_platform_ioremap_resource(pdev, 0);
@@ -887,14 +883,13 @@ static int airoha_npu_probe(struct platf
 		INIT_WORK(&core->wdt_work, airoha_npu_wdt_work);
 	}
 
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
-	for( irq_idx = 0; irq_idx < NPU_INT_MAX_NUM; irq_idx++ ){
-		irq = platform_get_irq(pdev, irq_idx + NPU_NUM_CORES + 1);
-		if (irq <= 0)
+	for (i = 0; i < ARRAY_SIZE(npu_glb->irqs); i++) {
+		irq = platform_get_irq(pdev, i + ARRAY_SIZE(npu_glb->cores) + 1);
+		if (irq < 0)
 			return irq;
-		npu_glb->irq[irq_idx] = irq;
-	}	
-#endif
+
+		npu_glb->irqs[i] = irq;
+	}
 
 	err = dma_set_coherent_mask(dev, DMA_BIT_MASK(32));
 	if (err)
--- a/drivers/net/ethernet/airoha/hostadpt.c
+++ b/drivers/net/ethernet/airoha/hostadpt.c
@@ -555,7 +555,7 @@ void hostadpt_regiter_interrupt(u32 irq_
 		HOSTADPT_MSG(DBG_ERR, "irq num error, valid irq num : 0 - 5 \n");
 		return ;
 	}
-	if(request_irq(npu_glb->irq[irq_num], int_isr, 0, irq_name, NULL)!=0)
+	if(request_irq(npu_glb->irqs[irq_num], int_isr, 0, irq_name, NULL)!=0)
 	{
 		printk("request_irq failed.\n");
 	}
@@ -571,7 +571,7 @@ void hostadpt_free_interrupt(u32 irq_num
 		HOSTADPT_MSG(DBG_ERR, "irq num error, valid irq num : 0 - 5 \n");
 		return ;
 	}
-	free_irq(npu_glb->irq[irq_num], NULL);
+	free_irq(npu_glb->irqs[irq_num], NULL);
 	return ;
 }
 
--- a/include/linux/soc/airoha/airoha_npu_g.h
+++ b/include/linux/soc/airoha/airoha_npu_g.h
@@ -44,7 +44,6 @@ struct airoha_npu {
 	struct device *dev;
 	struct regmap *regmap;
 	void __iomem *Regbase;
-	int irq[NPU_INT_MAX_NUM];
 
 	struct airoha_npu_core {
 		struct airoha_npu *npu;
@@ -53,6 +52,8 @@ struct airoha_npu {
 		struct work_struct wdt_work;
 	} cores[NPU_NUM_CORES];
 
+	int irqs[NPU_INT_MAX_NUM];
+
 	struct airoha_foe_stats *stats;
 
 	struct {
