--- a/drivers/net/ethernet/airoha/airoha_npu.c
+++ b/drivers/net/ethernet/airoha/airoha_npu.c
@@ -1148,7 +1148,7 @@ static int airoha_npu_probe(struct platf
 
 	platform_set_drvdata(pdev, npu);
 
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 	hostadpInit(npu);
 #endif
 
--- a/drivers/misc/Kconfig
+++ b/drivers/misc/Kconfig
@@ -562,12 +562,6 @@ config TPS6594_PFSM
 	  This driver can also be built as a module.  If so, the module
 	  will be called tps6594-pfsm.
 	  
-config NET_AIROHA_SOC_NPU
-	bool "MediaTek devices"
-	default y
-	help
-	  If you have a wireless npu belonging to this class, say Y.	  
-
 source "drivers/misc/c2port/Kconfig"
 source "drivers/misc/eeprom/Kconfig"
 source "drivers/misc/cb710/Kconfig"
