--- a/agg-rx.c
+++ b/agg-rx.c
@@ -4,7 +4,7 @@
  */
 #include "mt76.h"
 #include "trace.h"
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #include "npu.h"
 #endif
 static unsigned long mt76_aggr_tid_to_timeo(u8 tidno)
@@ -177,7 +177,7 @@ void mt76_rx_aggr_reorder(struct mt76_de
 	ackp = status->qos_ctl & IEEE80211_QOS_CTL_ACK_POLICY_MASK;
 	if (ackp == IEEE80211_QOS_CTL_ACK_POLICY_NOACK)
 		return;
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 	if(airoha_npu_device_active(dev->mmio.npu))
 		return;
 #endif
--- a/dma.c
+++ b/dma.c
@@ -6,7 +6,7 @@
 #include <linux/dma-mapping.h>
 #include "mt76.h"
 #include "dma.h"
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #include "npu.h"
 #endif
 
@@ -401,7 +401,7 @@ mt76_dma_tx_cleanup_idx(struct mt76_dev
 static void
 mt76_dma_kick_queue(struct mt76_dev *dev, struct mt76_queue *q)
 {
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 	if(airoha_npu_device_active(dev->mmio.npu)){
 		if(!(dev->phy.q_tx[0] == q ||(dev->phys[1] && dev->phys[1]->q_tx[0] == q))){
 			wmb();
@@ -753,7 +753,7 @@ mt76_dma_tx_queue_skb(struct mt76_phy *p
 		goto unmap;
 
 	phy->tx_dbg_stats.tx_to_hw++;
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
     if(airoha_npu_device_active(dev->mmio.npu)){
 		npu_mt76_dma_tx_queue_skb(phy, dev, t, skb, txwi, wcid, sta);
 		return -1;
--- a/npu.c
+++ b/npu.c
@@ -1,6 +1,6 @@
 #include "npu.h"
 
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 
 unsigned long int  *dramReorderNodeBase;
 #if !MT76_RROV31
--- a/npu.h
+++ b/npu.h
@@ -1,7 +1,7 @@
 #ifndef NPU_OFFLOAD_H
 #define NPU_OFFLOAD_H	
 
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 
 #include "mt7996/mt7996.h"
 #include "dma.h"
--- a/tx.c
+++ b/tx.c
@@ -868,7 +868,7 @@ int mt76_token_consume(struct mt76_dev *
 	if (mtk_wed_device_active(&dev->mmio.wed))
 		start = dev->mmio.wed.wlan.nbuf;
 	#endif
-	#ifdef CONFIG_NET_AIROHA_SOC_NPU
+	#ifdef CONFIG_NET_AIROHA_NPU
 		start = 16384;//NPU_TX_TOKEN_CNT;
 	#endif
 	spin_lock_bh(&dev->token_lock);
--- a/mt76.h
+++ b/mt76.h
@@ -676,7 +676,7 @@ struct mt76_driver_ops {
 
 	void (*sta_remove)(struct mt76_dev *dev, struct ieee80211_vif *vif,
 			   struct ieee80211_sta *sta);
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 	void (*npu_txwi_free)(struct mt76_dev *dev, struct mt76_txwi_cache *t,
 			 struct ieee80211_sta *sta, struct mt76_wcid *wcid,
 			 struct list_head *free_list);
--- a/mt7996/init.c
+++ b/mt7996/init.c
@@ -13,7 +13,7 @@
 #include "mcu.h"
 #include "coredump.h"
 #include "eeprom.h"
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #include "../npu.h"
 #endif
 
@@ -1096,7 +1096,7 @@ static int mt7996_wed_rro_init(struct mt
 	struct mt7996_wed_rro_addr *addr;
 	void *ptr;
 	int i;
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #if !MT76_RROV31
 	//struct airoha_npu *npu = airoha_npu_get(glb_eth->dev);
 	int isMailBoxSuccess=0;
@@ -1137,7 +1137,7 @@ static int mt7996_wed_rro_init(struct mt
 			addr->signature = 0xff;
 			addr++;
 		}
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #if !MT76_RROV31
 		if(airoha_npu_device_active(npu) && !(IS_ERR(npu)))
 			struct airoha_npu *npu = dev->mt76.mmio.npu;
@@ -1177,7 +1177,7 @@ static int mt7996_wed_rro_init(struct mt
 		addr->signature = 0xff;
 		addr++;
 	}
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #if !MT76_RROV31
 	if(airoha_npu_device_active(npu) && !(IS_ERR(npu)))
 		isMailBoxSuccess = npu->ops.wlan_set_txrx_reg_addr(npu, 1,
@@ -1259,7 +1259,7 @@ static void mt7996_wed_rro_work(struct w
 		e = list_first_entry(&list, struct mt7996_wed_rro_session_id,
 				     list);
 		list_del_init(&e->list);
-#ifdef CONFIG_NET_AIROHA_SOC_NPU		
+#ifdef CONFIG_NET_AIROHA_NPU		
 		if(airoha_npu_device_active(dev->mt76.mmio.npu)){
 			#if !MT76_RROV31
 	  		npu_rro_reset_sessions(dev, e->id);
--- a/mt7996/mt7996.h
+++ b/mt7996/mt7996.h
@@ -1455,7 +1455,7 @@ int mt7996_tx_prepare_skb(struct mt76_de
 void mt7996_tx_token_put(struct mt7996_dev *dev);
 void mt7996_queue_rx_skb(struct mt76_dev *mdev, enum mt76_rxq_id q,
 			 struct sk_buff *skb, u32 *info);
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 void npu_mt7996_txwi_free(struct mt76_dev *dev, struct mt76_txwi_cache *t,
 		 struct ieee80211_sta *sta, struct mt76_wcid *wcid,	 struct list_head *free_list);
 #endif
--- a/mt7996/mtk_debugfs.c
+++ b/mt7996/mtk_debugfs.c
@@ -10,7 +10,7 @@
 #include "mtk_debug.h"
 #include "mtk_mcu.h"
 #include "coredump.h"
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #include "../npu.h"
 #endif
 
--- a/mt7996/dma.c
+++ b/mt7996/dma.c
@@ -6,7 +6,7 @@
 #include "mt7996.h"
 #include "../dma.h"
 #include "mac.h"
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #include "../npu.h"
 #endif
 int mt7996_init_tx_queues(struct mt7996_phy *phy, int idx, int n_desc,
@@ -623,7 +623,7 @@ start_hw_rro:
 			mt76_queue_rx_init(dev, MT_RXQ_MSDU_PAGE_BAND1, mt76_dma_rx_poll);
 			mt76_queue_rx_init(dev, MT_RXQ_MSDU_PAGE_BAND2, mt76_dma_rx_poll);
 		}
-#ifndef CONFIG_NET_AIROHA_SOC_NPU		
+#ifndef CONFIG_NET_AIROHA_NPU		
 		mt76_queue_rx_init(dev, MT_RXQ_RRO_BAND0, mt76_dma_rx_poll);
 		mt76_queue_rx_init(dev, MT_RXQ_RRO_BAND1, mt76_dma_rx_poll);
 #endif		
--- a/mt7996/mac.c
+++ b/mt7996/mac.c
@@ -13,7 +13,7 @@
 #include "vendor.h"
 #include "mt7996_trace.h"
 #include "debug.h"
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #include "../npu.h"
 #endif
 
@@ -336,7 +336,7 @@ mt7996_mac_fill_rx(struct mt7996_dev *de
 
 	band_idx = FIELD_GET(MT_RXD1_NORMAL_BAND_IDX, rxd1);
 	mphy = dev->mt76.phys[band_idx];
-#ifdef CONFIG_NET_AIROHA_SOC_NPU	
+#ifdef CONFIG_NET_AIROHA_NPU	
 	if(!mphy){
 		printk("=======mt7996_mac_fill_rx!!! band_idx = %d =====\n", band_idx);	
 		return -EINVAL;
@@ -1238,7 +1238,7 @@ out:
 }
 
 
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 void npu_mt7996_txwi_free(struct mt76_dev *dev, struct mt76_txwi_cache *t,
 		 struct ieee80211_sta *sta, struct mt76_wcid *wcid,
 		 struct list_head *free_list)
--- a/mt7996/mmio.c
+++ b/mt7996/mmio.c
@@ -15,7 +15,7 @@
 #include "../dma.h"
 #include "../npu.h"
 
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #include "../npu.h"
 #endif
 
@@ -732,7 +732,7 @@ static void mt7996_rx_poll_complete(stru
 	struct mt7996_dev *dev = container_of(mdev, struct mt7996_dev, mt76);
 	struct airoha_npu *npu = dev->mt76.mmio.npu;
 	//struct airoha_npu *npu = airoha_npu_get(glb_eth->dev);
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
     if(airoha_npu_device_active(npu)){
 #if MT76_RROV31
         if(q != MT_RXQ_RRO_RXDMAD_C)//wifi host not rcv data pkts after enable npu  rro3.1 
@@ -813,7 +813,7 @@ static void mt7996_irq_tasklet(struct ta
 
 	for (i = 0; i < __MT_RXQ_MAX; i++) {
 		if ((intr & MT_INT_RX(i))){
-#ifdef CONFIG_NET_AIROHA_SOC_NPU	
+#ifdef CONFIG_NET_AIROHA_NPU	
 			if(airoha_npu_device_active(dev->mt76.mmio.npu)){
 				if (i != MT_RXQ_MAIN_WA)
 					napi_schedule(&dev->mt76.napi[i]);				
@@ -893,7 +893,7 @@ struct mt7996_dev *mt7996_mmio_probe(str
 		.sta_remove = mt7996_mac_sta_remove,
 		.update_survey = mt7996_update_channel,
 		.set_channel = mt7996_set_channel,
-#ifdef CONFIG_NET_AIROHA_SOC_NPU	
+#ifdef CONFIG_NET_AIROHA_NPU	
 		.npu_txwi_free = npu_mt7996_txwi_free,
 #endif			
 	};
--- a/mt7996/pci.c
+++ b/mt7996/pci.c
@@ -9,7 +9,7 @@
 #include "mt7996.h"
 #include "mac.h"
 #include "../trace.h"
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
 #include "../npu.h"
 #endif
 
@@ -230,7 +230,7 @@ static int mt7996_pci_probe(struct pci_d
 	ret = mt7996_register_device(dev);
 	if (ret)
 		goto free_hif2_irq;
-#ifdef CONFIG_NET_AIROHA_SOC_NPU
+#ifdef CONFIG_NET_AIROHA_NPU
     if(airoha_npu_device_active(dev->mt76.mmio.npu)){		
 			wifi_offload_init_npu(pdev,dev,0);
 			get_npu_rxd(dev,pdev);
