diff --git a/mt76.h b/mt76.h
index 0146da6a..9fb06e6a 100644
--- a/mt76.h
+++ b/mt76.h
@@ -1972,6 +1972,10 @@ mt76_tx_status_get_hw(struct mt76_dev *dev, struct sk_buff *skb)
 	return hw;
 }
 
+int mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
+			    u32 direction, u32 i_count_addr,
+			    u32 o_status_addr, u32 o_count_addr);
+
 void mt76_put_txwi(struct mt76_dev *dev, struct mt76_txwi_cache *t);
 void mt76_put_rxwi(struct mt76_dev *dev, struct mt76_txwi_cache *t);
 struct mt76_txwi_cache *mt76_get_rxwi(struct mt76_dev *dev);
diff --git a/mt7996/init.c b/mt7996/init.c
index 63c0480e..ee192885 100644
--- a/mt7996/init.c
+++ b/mt7996/init.c
@@ -81,10 +81,8 @@ static const struct ieee80211_iface_combination if_comb_7992 = {
 static const u8 mt7996_if_types_ext_capa_ap[] = {
 	[0] = WLAN_EXT_CAPA1_EXT_CHANNEL_SWITCHING,
 	[2] = WLAN_EXT_CAPA3_MULTI_BSSID_SUPPORT,
-	[4] = WLAN_EXT_CAPA5_QOS_MAP,
 	[6] = WLAN_EXT_CAPA7_SCS_SUPPORT,
 	[7] = WLAN_EXT_CAPA8_OPMODE_NOTIF,
-	[10] = WLAN_EXT_CAPA11_MIRRORED_SCS_SUPPORT,
 };
 
 static const u8 mt7996_if_types_ext_capa_sta[] = {
@@ -1155,7 +1153,7 @@ static int mt7996_wed_rro_init(struct mt7996_dev *dev)
 		}
 		
 		if (is_mt7996(&dev->mt76) && mt76_npu_device_active(&dev->mt76)){
-			if (airoha_npu_wlan_set_txrx_reg_addr(npu, 0, i, dev->wed_rro.addr_elem[i].phy_addr, 0, 0))
+			if (mt76_npu_send_txrx_addr(&dev->mt76, 0, i, dev->wed_rro.addr_elem[i].phy_addr, 0, 0))
 				dev_err(dev->mt76.dev, "failed setting npu wlan txrx addr0\n");
 		}
 
@@ -1214,7 +1212,7 @@ static int mt7996_wed_rro_init(struct mt7996_dev *dev)
 		addr++;
 	}
 	if (is_mt7996(&dev->mt76) && mt76_npu_device_active(&dev->mt76)){
-		if (airoha_npu_wlan_set_txrx_reg_addr(npu, 1, 0, dev->wed_rro.session.phy_addr, 0, 0))
+		if (mt76_npu_send_txrx_addr(&dev->mt76, 1, 0, dev->wed_rro.session.phy_addr, 0, 0))
 			dev_err(dev->mt76.dev, "failed setting npu wlan txrx addr1\n");
 	}
 	/* rro ind cmd queue init */
@@ -1320,7 +1318,7 @@ static void mt7996_wed_rro_work(struct work_struct *work)
 
 		if (mt76_npu_device_active(&dev->mt76)){
             if (is_mt7996(&dev->mt76)){
-				if (airoha_npu_wlan_set_txrx_reg_addr(npu, 3, e->id, 0, 0, 0))
+				if (mt76_npu_send_txrx_addr(&dev->mt76, 3, e->id, 0, 0, 0))
 					dev_err(dev->mt76.dev, "failed setting npu wlan txrx addr3\n");	
 			}	
 			goto reset_session;
@@ -1855,9 +1853,6 @@ mt7996_init_eht_caps(struct mt7996_phy *phy, enum nl80211_band band,
 	if (iftype != NL80211_IFTYPE_AP)
 		return;
 
-	eht_cap_elem->mac_cap_info[1] |=
-		IEEE80211_EHT_MAC_CAP1_UNSOL_EPCS_PRIO_ACCESS;
-
 	eht_cap_elem->phy_cap_info[3] |=
 		IEEE80211_EHT_PHY_CAP3_TRIG_SU_BF_FDBK |
 		IEEE80211_EHT_PHY_CAP3_TRIG_MU_BF_PART_BW_FDBK;
diff --git a/mt7996/main.c b/mt7996/main.c
index 40b7a6be..1e9781d7 100644
--- a/mt7996/main.c
+++ b/mt7996/main.c
@@ -2920,6 +2920,7 @@ out:
 	return ret;
 }
 
+#if 0
 static int
 mt7996_set_ttlm(struct ieee80211_hw *hw, struct ieee80211_vif *vif)
 {
@@ -2972,6 +2973,7 @@ mt7996_set_attlm(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
 	mutex_unlock(&dev->mt76.mutex);
 	return ret;
 }
+#endif
 
 static enum ieee80211_neg_ttlm_res
 mt7996_can_neg_ttlm(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
@@ -3185,9 +3187,9 @@ const struct ieee80211_ops mt7996_ops = {
 	.change_vif_links = mt7996_change_vif_links,
 	.change_sta_links = mt7996_mac_sta_change_links,
 	.set_qos_map = mt7996_set_qos_map,
-	.set_attlm = mt7996_set_attlm,
-	.set_sta_ttlm = mt7996_set_sta_ttlm,
+	//.set_attlm = mt7996_set_attlm,
+	//.set_sta_ttlm = mt7996_set_sta_ttlm,
 	.can_neg_ttlm = mt7996_can_neg_ttlm,
-	.set_ttlm = mt7996_set_ttlm,
+	//.set_ttlm = mt7996_set_ttlm,
 	.channel_switch = mt7996_sta_channel_switch,
 };
diff --git a/mt7996/mcu.c b/mt7996/mcu.c
index 9b0acfea..824614cb 100644
--- a/mt7996/mcu.c
+++ b/mt7996/mcu.c
@@ -750,7 +750,7 @@ static inline void __mt7996_stat_to_netdev(struct mt76_phy *mphy,
 			return;
 
 		dev_sw_netstats_tx_add(wdev->netdev, tx_packets, tx_bytes);
-		__dev_sw_netstats_rx_add(wdev->netdev, rx_packets, rx_bytes);
+		//__dev_sw_netstats_rx_add(wdev->netdev, rx_packets, rx_bytes);
 	}
 }
 
@@ -4052,8 +4052,8 @@ mt7996_mcu_beacon_crit_update(struct sk_buff *rskb, struct sk_buff *skb,
 	int i;
 
 	if (!ieee80211_vif_is_mld(conf->vif) ||
-	    !(capab_info & (WLAN_CAPABILITY_PBCC |
-			    WLAN_CAPABILITY_NON_TX_BSSID_CU)))
+	    !(capab_info & (WLAN_CAPABILITY_PBCC /*|
+			    WLAN_CAPABILITY_NON_TX_BSSID_CU*/)))
 		return;
 
 	tlv = mt7996_mcu_add_uni_tlv(rskb, UNI_BSS_INFO_BCN_CRIT_UPDATE, sizeof(*crit));
diff --git a/mt7996/mtk_debugfs.c b/mt7996/mtk_debugfs.c
index 336bd848..263e3c3e 100644
--- a/mt7996/mtk_debugfs.c
+++ b/mt7996/mtk_debugfs.c
@@ -3661,10 +3661,10 @@ void npu_wifi_offload_get_dbg_counter_address(struct mt7996_dev *dev)
 	printk("%s:%d !!!\n",__func__,__LINE__);
     for(i = 0; i< (BAND_NUM-1); i++)
     {
-		if (airoha_npu_wlan_get_dbg_counter_address(npu, i, &val)) {
-			printk("%s:%d band:%d get dbg counter addr error \n",__func__,__LINE__,i);
-			return;
-		} 
+		//if (airoha_npu_wlan_get_dbg_counter_address(npu, i, &val)) {
+		//	printk("%s:%d band:%d get dbg counter addr error \n",__func__,__LINE__,i);
+		//	return;
+		//} 
         if(i == 2)
         {
             
diff --git a/mt7996/pci.c b/mt7996/pci.c
index 1b791c73..adabfa3f 100644
--- a/mt7996/pci.c
+++ b/mt7996/pci.c
@@ -115,11 +115,9 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 	phys_addr_t paddr = pci_resource_start(pdev, 0);
 	u32 hif1_ofs = 0;
 	int err;
-	u32 addr;
+	u32 addr, val;
 	
-	err = airoha_npu_wlan_send_msg(npu, 0,
-									WLAN_FUNC_SET_WAIT_PCIE_PORT_TYPE,
-									type, GFP_ATOMIC);
+	err = airoha_npu_wlan_send_msg(npu, 0, WLAN_FUNC_SET_WAIT_PCIE_PORT_TYPE, &type, sizeof(type), GFP_ATOMIC);
 	if (err) {
 		dev_warn(dev->mt76.dev,
 			 "failed setting npu wlan PCIe port type\n");
@@ -140,9 +138,7 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			else
 				addr += MT_RXQ_RING_BASE(MT_RXQ_RRO_BAND0) + 0x80;
 
-			err = airoha_npu_wlan_send_msg(npu, i, 
-											WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-											addr, GFP_ATOMIC);
+			err = airoha_npu_wlan_send_msg(npu, i, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &addr, sizeof(addr), GFP_ATOMIC);
 			if (err) {
 
 				//printk("%s:%d failed setting npu wlan PCIe desc addr !!!!!\n",__func__,__LINE__);
@@ -151,8 +147,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 				return err;
 			}
 
-			err = airoha_npu_wlan_send_msg(npu, i, WLAN_FUNC_SET_WAIT_DESC,
-											MT7996_RX_RING_SIZE, GFP_ATOMIC);
+			val = MT7996_RX_RING_SIZE;
+			err = airoha_npu_wlan_send_msg(npu, i, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 			if (err) {
 
 				//printk("%s:%d failed setting npu wlan PCIe desc size !!!!!\n",__func__,__LINE__);
@@ -167,9 +163,7 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			else
 				addr += MT_TXQ_RING_BASE(0) + 0x120;
 
-			err = airoha_npu_wlan_send_msg(npu, i,
-											WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR,
-											addr, GFP_ATOMIC);
+			err = airoha_npu_wlan_send_msg(npu, i, WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR, &addr, sizeof(addr), GFP_ATOMIC);
 			if (err) {
 				dev_warn(dev->mt76.dev,
 					 "failed setting npu wlan tx desc addr\n");
@@ -179,9 +173,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 
 		/*rxdmad_c ring*/
 		printk("%s:%d setting npu wlan rxdmad_c addr !!!!!\n",__func__,__LINE__);
-		err = airoha_npu_wlan_send_msg(npu, 9, WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-										paddr + MT_RXQ_RRO_AP_RING_BASE,
-										GFP_ATOMIC);
+		val = paddr + MT_RXQ_RRO_AP_RING_BASE;
+		err = airoha_npu_wlan_send_msg(npu, 9, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			//printk("%s:%d failed setting npu wlan rxdmad_c addr !!!!!\n",__func__,__LINE__);
 			dev_warn(dev->mt76.dev,
@@ -189,17 +182,16 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			return err;
 		}
 
-		err = airoha_npu_wlan_send_msg(npu, 9, WLAN_FUNC_SET_WAIT_DESC,
-										MT7996_RX_RING_SIZE, GFP_ATOMIC);
+		val = MT7996_RX_RING_SIZE;
+		err = airoha_npu_wlan_send_msg(npu, 9, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed setting npu wlan rxdmad_c desc size\n");
 			return err;
 		}
 
-		err = airoha_npu_wlan_send_msg(npu, 2,
-										WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR,
-										paddr + MT_RRO_ACK_SN_CTRL, GFP_ATOMIC);
+		val = paddr + MT_RRO_ACK_SN_CTRL;
+		err = airoha_npu_wlan_send_msg(npu, 2, WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 		if (err)
 			dev_warn(dev->mt76.dev,
 				 "failed setting npu wlan rro_ack_sn desc addr\n");
@@ -212,8 +204,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 		//isMailBoxSuccess = WIFI_MAIL_API_SET_WAIT_PCIE_ADDR(band_idx, (pcie_pa + 0xd4000 + 0x580));
 		//isMailBoxSuccess = WIFI_MAIL_API_SET_WAIT_PCIE_ADDR(band_idx, (pcie_pa + MT_RXQ_RING_BASE(MT_RXQ_RRO_BAND0) + 0x80));
 
-		err = airoha_npu_wlan_send_msg(npu, band_idx, WLAN_FUNC_SET_WAIT_DESC,
-										NPU_RX_RING_0_SIZE, GFP_ATOMIC);
+		val = NPU_RX_RING_0_SIZE;
+		err = airoha_npu_wlan_send_msg(npu, band_idx, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 		
 			//printk("%s:%d failed setting npu wlan PCIe desc size !!!!!\n",__func__,__LINE__);
@@ -227,8 +219,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 		//isMailBoxSuccess = WIFI_MAIL_API_SET_WAIT_PCIE_ADDR(band_idx + 2, 
 		//													(pcie_pa + MT_RXQ_RING_BASE(MT_RXQ_RRO_BAND2) + 0x60 + hif1_ofs));
 
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 2, WLAN_FUNC_SET_WAIT_DESC,
-										NPU_RX_RING_1_SIZE, GFP_ATOMIC);
+		val = NPU_RX_RING_1_SIZE;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 2, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 		
 			//printk("%s:%d failed setting npu wlan PCIe desc size !!!!!\n",__func__,__LINE__);
@@ -239,9 +231,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			
 		/* MSDU PG*/
 		band_idx = 0;
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, 
-										WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-										paddr + MT_RXQ_RING_BASE(MT_RXQ_MSDU_PAGE_BAND0) + 0xa0, GFP_ATOMIC);
+		val = paddr + MT_RXQ_RING_BASE(MT_RXQ_MSDU_PAGE_BAND0) + 0xa0;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 	
 		if (err) {
 			dev_warn(dev->mt76.dev,
@@ -249,8 +240,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			return err;
 		}
 
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_DESC,
-										NPU_MSDU_PAGE_RING_2G_SIZE, GFP_ATOMIC);
+		val = NPU_MSDU_PAGE_RING_2G_SIZE;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 		
 			//printk("%s:%d failed setting npu wlan PCIe desc size !!!!!\n",__func__,__LINE__);
@@ -260,9 +251,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 		}	
 
 		band_idx = 1;
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, 
-										WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-										paddr + MT_RXQ_RING_BASE(MT_RXQ_MSDU_PAGE_BAND1) + 0xb0, GFP_ATOMIC);
+		val = paddr + MT_RXQ_RING_BASE(MT_RXQ_MSDU_PAGE_BAND1) + 0xb0;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 	
 		if (err) {
 			dev_warn(dev->mt76.dev,
@@ -270,8 +260,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			return err;
 		}
 
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_DESC,
-										NPU_MSDU_PAGE_RING_5G_SIZE, GFP_ATOMIC);
+		val = NPU_MSDU_PAGE_RING_5G_SIZE;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 		
 			//printk("%s:%d failed setting npu wlan PCIe desc size !!!!!\n",__func__,__LINE__);
@@ -281,9 +271,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 		}	
 		
 		band_idx = 2;
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, 
-										WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-										paddr + MT_RXQ_RING_BASE(MT_RXQ_MSDU_PAGE_BAND2) + 0xc0, GFP_ATOMIC);
+		val = paddr + MT_RXQ_RING_BASE(MT_RXQ_MSDU_PAGE_BAND2) + 0xc0;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 	
 		if (err) {
 			dev_warn(dev->mt76.dev,
@@ -291,8 +280,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			return err;
 		}
 		
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_DESC,
-										NPU_MSDU_PAGE_RING_6G_SIZE, GFP_ATOMIC);
+		val = NPU_MSDU_PAGE_RING_6G_SIZE;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 5, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 		
 			//printk("%s:%d failed setting npu wlan PCIe desc size !!!!!\n",__func__,__LINE__);
@@ -303,9 +292,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			
 		/* ind_cmd ring*/
 		band_idx = 0;
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 8, 
-										WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-										paddr + MT_RXQ_RRO_IND_RING_BASE, GFP_ATOMIC);
+		val = paddr + MT_RXQ_RRO_IND_RING_BASE;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 8, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 	
 		if (err) {
 			dev_warn(dev->mt76.dev,
@@ -313,8 +301,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			return err;
 		}
 		
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 8, WLAN_FUNC_SET_WAIT_DESC,
-										NPU_RRO_IND_RING, GFP_ATOMIC);
+		val = NPU_RRO_IND_RING;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 8, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 		
 			//printk("%s:%d failed setting npu wlan PCIe desc size !!!!!\n",__func__,__LINE__);
@@ -323,9 +311,8 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 			return err;
 		}	
 
-		err = airoha_npu_wlan_send_msg(npu, 3,
-										WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR,
-										paddr + MT_RRO_ACK_SN_CTRL, GFP_ATOMIC);
+		val = paddr + MT_RRO_ACK_SN_CTRL;
+		err = airoha_npu_wlan_send_msg(npu, 3, WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed setting npu wlan rro_ack_sn desc addr\n");
@@ -335,18 +322,16 @@ static int mt7996_npu_wlan_offload_init(struct mt7996_dev *dev,
 		/* npu tx */
 		//isMailBoxSuccess = WIFI_MAIL_API_SET_WAIT_TX_RING_PCIE_ADDR(band_idx, (pcie_pa + MT_TXQ_RING_BASE(0) + 0x130 + hif1_ofs));
 
-		err = airoha_npu_wlan_send_msg(npu, band_idx,
-										WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR,
-										paddr + MT_TXQ_RING_BASE(1) + 0x120, GFP_ATOMIC);      //T18
+		val = paddr + MT_TXQ_RING_BASE(1) + 0x120;
+		err = airoha_npu_wlan_send_msg(npu, band_idx, WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed setting npu tx ring pcie addr\n");
 			return err;
 		}	
 
-		err = airoha_npu_wlan_send_msg(npu, band_idx + 2,
-										WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR,
-										paddr + MT_TXQ_RING_BASE(0) + 0x150 + hif1_ofs, GFP_ATOMIC);      //T21
+		val = paddr + MT_TXQ_RING_BASE(0) + 0x150 + hif1_ofs;
+		err = airoha_npu_wlan_send_msg(npu, band_idx + 2, WLAN_FUNC_SET_WAIT_TX_RING_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed setting npu tx ring pcie addr\n");
@@ -363,7 +348,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 	if (is_mt7992(&dev->mt76))
 	{
 		err = airoha_npu_wlan_get_msg(npu, 0, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);
+					      &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan rx ring0 addr\n");
@@ -372,7 +357,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 		writel(val, &dev->mt76.q_rx[MT_RXQ_RRO_BAND0].regs->desc_base);
 
 		err = airoha_npu_wlan_get_msg(npu, 1, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);
+					      &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan rx ring1 addr\n");
@@ -381,7 +366,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 		writel(val, &dev->mt76.q_rx[MT_RXQ_RRO_BAND1].regs->desc_base);
 
 		err = airoha_npu_wlan_get_msg(npu, 9, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);
+					      &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan rxdmad_c ring addr\n");
@@ -392,7 +377,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 	else if (is_mt7996(&dev->mt76))
 	{
 		err = airoha_npu_wlan_get_msg(npu, 0, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);	
+					      &val, sizeof(val), GFP_ATOMIC);	
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan rx ring0 addr\n");
@@ -401,7 +386,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 		
 		writel(val, &dev->mt76.q_rx[MT_RXQ_RRO_BAND0].regs->desc_base);
 		err = airoha_npu_wlan_get_msg(npu, 2, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);
+					      &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan rx ring2 addr\n");
@@ -411,7 +396,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 
 		/* MSDU PG ring */
 		err = airoha_npu_wlan_get_msg(npu, 10, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);
+					      &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan MSDU PG base addr\n");
@@ -420,7 +405,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 		writel(val, &dev->mt76.q_rx[MT_RXQ_MSDU_PAGE_BAND0].regs->desc_base);
 		
 		err = airoha_npu_wlan_get_msg(npu, 11, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);
+					      &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan MSDU PG base addr\n");
@@ -429,7 +414,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 		writel(val, &dev->mt76.q_rx[MT_RXQ_MSDU_PAGE_BAND1].regs->desc_base);	
 
 		err = airoha_npu_wlan_get_msg(npu, 12, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);
+					      &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan MSDU PG base addr\n");
@@ -440,7 +425,7 @@ static int mt7996_npu_init_rxd(struct mt7996_dev *dev, struct airoha_npu *npu)
 	
 		/* ind_cmd ring */
 		err = airoha_npu_wlan_get_msg(npu, 8, WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-					      &val);
+					      &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed retriving npu wlan ind_cmd ring base addr\n");
@@ -466,7 +451,7 @@ static int mt7996_npu_init_txd(struct mt7996_dev *dev, struct airoha_npu *npu,
 
 			err = airoha_npu_wlan_get_msg(npu, i + 5,
 						      WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-						      &val);
+						      &val, sizeof(val), GFP_ATOMIC);
 			if (err) {
 				dev_warn(dev->mt76.dev,
 					 "failed retriving npu wlan tx ring addr\n");
@@ -474,27 +459,30 @@ static int mt7996_npu_init_txd(struct mt7996_dev *dev, struct airoha_npu *npu,
 			}
 			writel(val, &dev->mt76.phys[i]->q_tx[0]->regs->desc_base);
 
+			val = dma_addr_list[index];
 			err = airoha_npu_wlan_send_msg(npu, i,
 						       WLAN_FUNC_SET_WAIT_TX_BUF_SPACE_HW_BASE,
-						       dma_addr_list[index], GFP_ATOMIC);
+						       &val, sizeof(val), GFP_ATOMIC);
 			if (err) {
 				dev_warn(dev->mt76.dev,
 					 "failed setting npu wlan queue buf addr\n");
 				return err;
 			}
 
+			val = dma_addr_list[index + 1];
 			err = airoha_npu_wlan_send_msg(npu, i + 5,
 						       WLAN_FUNC_SET_WAIT_TX_BUF_SPACE_HW_BASE,
-						       dma_addr_list[index + 1], GFP_ATOMIC);
+						       &val, sizeof(val), GFP_ATOMIC);
 			if (err) {
 				dev_warn(dev->mt76.dev,
 					 "failed setting npu wlan tx buf addr\n");
 				return err;
 			}
 
+			val = dma_addr_list[index + 2];
 			err = airoha_npu_wlan_send_msg(npu, i + 10,
 						       WLAN_FUNC_SET_WAIT_TX_BUF_SPACE_HW_BASE,
-						       dma_addr_list[index + 2], GFP_ATOMIC);
+						       &val, sizeof(val), GFP_ATOMIC);
 			if (err) {
 				dev_warn(dev->mt76.dev,
 					 "failed setting npu wlan tx buf base\n");
@@ -509,7 +497,7 @@ static int mt7996_npu_init_txd(struct mt7996_dev *dev, struct airoha_npu *npu,
 			if(i == MT_BAND0){
 				err = airoha_npu_wlan_get_msg(npu, i + 5,
 							      WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-							      &val);
+							      &val, sizeof(val), GFP_ATOMIC);
 				if (err) {
 					dev_warn(dev->mt76.dev,
 						 "failed retriving npu wlan tx ring addr\n");
@@ -520,7 +508,7 @@ static int mt7996_npu_init_txd(struct mt7996_dev *dev, struct airoha_npu *npu,
 			else if(i == MT_BAND2){
 				err = airoha_npu_wlan_get_msg(npu, i + 5,
 							      WLAN_FUNC_GET_WAIT_RXDESC_BASE,
-							      &val);
+							      &val, sizeof(val), GFP_ATOMIC);
 				if (err) {
 					dev_warn(dev->mt76.dev,
 						 "failed retriving npu wlan tx ring addr\n");
@@ -530,27 +518,30 @@ static int mt7996_npu_init_txd(struct mt7996_dev *dev, struct airoha_npu *npu,
 			}else
 			continue;
 			
+			val = dma_addr_list[index];
 			err = airoha_npu_wlan_send_msg(npu, i,
 						       WLAN_FUNC_SET_WAIT_TX_BUF_SPACE_HW_BASE,
-						       dma_addr_list[index], GFP_ATOMIC);
+						       &val, sizeof(val), GFP_ATOMIC);
 			if (err) {
 				dev_warn(dev->mt76.dev,
 					 "failed setting npu wlan queue buf addr\n");
 				return err;
 			}
 
+			val = dma_addr_list[index + 1];
 			err = airoha_npu_wlan_send_msg(npu, i + 5,
 						       WLAN_FUNC_SET_WAIT_TX_BUF_SPACE_HW_BASE,
-						       dma_addr_list[index + 1], GFP_ATOMIC);
+						       &val, sizeof(val), GFP_ATOMIC);
 			if (err) {
 				dev_warn(dev->mt76.dev,
 					 "failed setting npu wlan tx buf addr\n");
 				return err;
 			}
 
+			val = dma_addr_list[index + 2];
 			err = airoha_npu_wlan_send_msg(npu, i + 10,
 						       WLAN_FUNC_SET_WAIT_TX_BUF_SPACE_HW_BASE,
-						       dma_addr_list[index + 2], GFP_ATOMIC);
+						       &val, sizeof(val), GFP_ATOMIC);
 			if (err) {
 				dev_warn(dev->mt76.dev,
 					 "failed setting npu wlan tx buf base\n");
@@ -569,11 +560,12 @@ static int mt7996_npu_init_rx_event(struct mt7996_dev *dev,
 	struct mt76_queue *q;
 	phys_addr_t paddr = pci_resource_start(pdev, 0);
 	int err;
+	u32 val;
+
 	if (is_mt7992(&dev->mt76)){
 		q =  &dev->mt76.q_rx[MT_RXQ_MAIN_WA];
-		err = airoha_npu_wlan_send_msg(npu, 0,
-										WLAN_FUNC_SET_WAIT_RX_RING_FOR_TXDONE_HW_BASE,
-										q->desc_dma, GFP_ATOMIC);
+		val = q->desc_dma;
+		err = airoha_npu_wlan_send_msg(npu, 0, WLAN_FUNC_SET_WAIT_RX_RING_FOR_TXDONE_HW_BASE, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 
 			dev_warn(dev->mt76.dev,
@@ -581,8 +573,8 @@ static int mt7996_npu_init_rx_event(struct mt7996_dev *dev,
 			return err;
 		}
 
-		err = airoha_npu_wlan_send_msg(npu, 10, WLAN_FUNC_SET_WAIT_DESC,
-										MT7996_RX_MCU_RING_SIZE, GFP_ATOMIC);
+		val = MT7996_RX_MCU_RING_SIZE;
+		err = airoha_npu_wlan_send_msg(npu, 10, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 
 			dev_warn(dev->mt76.dev,
@@ -591,17 +583,16 @@ static int mt7996_npu_init_rx_event(struct mt7996_dev *dev,
 		}
 
 		paddr += MT_RXQ_RING_BASE(MT_RXQ_MAIN_WA) + 0x20;
-		err = airoha_npu_wlan_send_msg(npu, 10, WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-										paddr, GFP_ATOMIC);
+		val = paddr;
+		err = airoha_npu_wlan_send_msg(npu, 10, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 		if (err)
 			dev_warn(dev->mt76.dev,
 				 "failed setting npu wlan rx pcie address\n");
 	}	
 	else if(is_mt7996(&dev->mt76)){	
 		q =  &dev->mt76.q_rx[MT_RXQ_TXFREE_BAND0];
-		err = airoha_npu_wlan_send_msg(npu, 0,
-										WLAN_FUNC_SET_WAIT_RX_RING_FOR_TXDONE_HW_BASE,
-										q->desc_dma, GFP_ATOMIC);
+		val = q->desc_dma;
+		err = airoha_npu_wlan_send_msg(npu, 0, WLAN_FUNC_SET_WAIT_RX_RING_FOR_TXDONE_HW_BASE, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 
 			dev_warn(dev->mt76.dev,
@@ -609,8 +600,8 @@ static int mt7996_npu_init_rx_event(struct mt7996_dev *dev,
 			return err;
 		}
 
-		err = airoha_npu_wlan_send_msg(npu, 10, WLAN_FUNC_SET_WAIT_DESC,
-										MT7996_RX_MCU_RING_SIZE, GFP_ATOMIC);
+		val = MT7996_RX_MCU_RING_SIZE;
+		err = airoha_npu_wlan_send_msg(npu, 10, WLAN_FUNC_SET_WAIT_DESC, &val, sizeof(val), GFP_ATOMIC);
 		if (err) {
 
 			dev_warn(dev->mt76.dev,
@@ -620,8 +611,8 @@ static int mt7996_npu_init_rx_event(struct mt7996_dev *dev,
 
 
 		paddr += MT_RXQ_RING_BASE(MT_RXQ_TXFREE_BAND0) + 0x90;
-		err = airoha_npu_wlan_send_msg(npu, 10, WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-										paddr, GFP_ATOMIC);
+		val = paddr;
+		err = airoha_npu_wlan_send_msg(npu, 10, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &val, sizeof(val), GFP_ATOMIC);
 		if (err)
 			dev_warn(dev->mt76.dev,
 				 "failed setting npu wlan rx pcie address\n");
@@ -714,6 +705,7 @@ static int mt7996_npu_set_pcie_addr(struct mt7996_dev *dev,
 {
 	int err;
 	u32 hif1_ofs = 0;
+	u32 addr;
 	
 	phys_addr_t paddr = pci_resource_start(pdev, 0);
 	u32 band_idx = 0;
@@ -723,9 +715,8 @@ static int mt7996_npu_set_pcie_addr(struct mt7996_dev *dev,
 	
 	/* npu rx rro ring*/
 	/*R8   -- refill ring0 5g+6g*/
-	err = airoha_npu_wlan_send_msg(npu, band_idx, 
-									WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-									paddr + MT_RXQ_RING_BASE(MT_RXQ_RRO_BAND0) + 0x80, GFP_ATOMIC); 
+	addr = paddr + MT_RXQ_RING_BASE(MT_RXQ_RRO_BAND0) + 0x80;
+	err = airoha_npu_wlan_send_msg(npu, band_idx, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &addr, sizeof(addr), GFP_ATOMIC); 
 	if (err) {
 		dev_warn(dev->mt76.dev,
 			 "failed setting npu wlan PCIe desc addr\n");
@@ -733,9 +724,8 @@ static int mt7996_npu_set_pcie_addr(struct mt7996_dev *dev,
 	}
 			
 	/*R6  -- refill ring1 2g*/
-	err = airoha_npu_wlan_send_msg(npu, band_idx + 2, 
-									WLAN_FUNC_SET_WAIT_PCIE_ADDR,
-									paddr + MT_RXQ_RING_BASE(MT_RXQ_RRO_BAND2) + 0x60 + hif1_ofs, GFP_ATOMIC);
+	addr = paddr + MT_RXQ_RING_BASE(MT_RXQ_RRO_BAND2) + 0x60 + hif1_ofs;
+	err = airoha_npu_wlan_send_msg(npu, band_idx + 2, WLAN_FUNC_SET_WAIT_PCIE_ADDR, &addr, sizeof(addr), GFP_ATOMIC);
 	if (err) {
 		dev_warn(dev->mt76.dev,
 			 "failed setting npu wlan PCIe desc addr\n");
@@ -748,11 +738,13 @@ static int mt7996_npu_set_pcie_addr(struct mt7996_dev *dev,
 static int mt7996_npu_init_tx_done(struct mt7996_dev *dev, struct airoha_npu *npu)
 {
 	int err;
+	u32 val = 0;
 
 	if(is_mt7996(&dev->mt76)){
 		/* rro ring set cpu idx */
-		err = airoha_npu_wlan_send_msg(npu, 15, 
-										WLAN_FUNC_SET_WAIT_PCIE_ADDR, 0, GFP_ATOMIC);
+		err = airoha_npu_wlan_send_msg(npu, 15,
+					       WLAN_FUNC_SET_WAIT_PCIE_ADDR, &val,
+					       sizeof(val), GFP_ATOMIC);
 		if (err) {
 			dev_warn(dev->mt76.dev,
 				 "failed setting npu wlan PCIe desc addr 15\n");
@@ -761,7 +753,7 @@ static int mt7996_npu_init_tx_done(struct mt7996_dev *dev, struct airoha_npu *np
 	}
 	
 	err = airoha_npu_wlan_send_msg(npu, 2,
-				       WLAN_FUNC_SET_WAIT_INODE_TXRX_REG_ADDR, 0,
+				       WLAN_FUNC_SET_WAIT_INODE_TXRX_REG_ADDR, &val, sizeof(val),
 				       GFP_ATOMIC);
 	if (err) {
 		dev_warn(dev->mt76.dev, "failed setting npu wlan txrx addr2\n");
@@ -769,7 +761,7 @@ static int mt7996_npu_init_tx_done(struct mt7996_dev *dev, struct airoha_npu *np
 	}
 
 	err = airoha_npu_wlan_send_msg(npu, 7,
-				       WLAN_FUNC_SET_WAIT_INODE_TXRX_REG_ADDR, 0,
+				       WLAN_FUNC_SET_WAIT_INODE_TXRX_REG_ADDR, &val, sizeof(val),
 				       GFP_ATOMIC);
 	if (err)
 		dev_warn(dev->mt76.dev, "failed setting npu wlan txrx addr7\n");
diff --git a/npu.c b/npu.c
index 37515e6f..61fbd638 100644
--- a/npu.c
+++ b/npu.c
@@ -462,6 +462,36 @@ int mt76_npu_net_setup_tc(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
 }
 EXPORT_SYMBOL_GPL(mt76_npu_net_setup_tc);
 
+int mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
+			    u32 direction, u32 i_count_addr,
+			    u32 o_status_addr, u32 o_count_addr)
+{
+	struct {
+		__le32 dir;
+		__le32 in_count_addr;
+		__le32 out_status_addr;
+		__le32 out_count_addr;
+	} info = {
+		.dir = cpu_to_le32(direction),
+		.in_count_addr = cpu_to_le32(i_count_addr),
+		.out_status_addr = cpu_to_le32(o_status_addr),
+		.out_count_addr = cpu_to_le32(o_count_addr),
+	};
+	struct airoha_npu *npu;
+	int err = -ENODEV;
+
+	rcu_read_lock();
+	npu = rcu_dereference(dev->mmio.npu);
+	if (npu)
+		err = airoha_npu_wlan_send_msg(npu, ifindex,
+				WLAN_FUNC_SET_WAIT_INODE_TXRX_REG_ADDR,
+				&info, sizeof(info), GFP_ATOMIC);
+	rcu_read_unlock();
+
+	return err;
+}
+EXPORT_SYMBOL_GPL(mt76_npu_send_txrx_addr);
+
 int mt76_npu_init(struct mt76_dev *dev)
 {
 	struct airoha_ppe_dev *ppe_dev;
@@ -473,10 +503,10 @@ int mt76_npu_init(struct mt76_dev *dev)
 	
 	mutex_lock(&dev->mutex);
 
-	npu = airoha_npu_get(dev->dev, NULL);
+	npu = airoha_npu_get(dev->dev);
 	if (IS_ERR(npu)) {
 		request_module("airoha-npu");
-		npu = airoha_npu_get(dev->dev, NULL);
+		npu = airoha_npu_get(dev->dev);
 	}
 
 	if (IS_ERR(npu)) {
